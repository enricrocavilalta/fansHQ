<h1 class="text-2xl text-neutral-100"><%= post.title %></h1>
<p class="text-neutral-300 whitespace-pre-wrap"><%= post.body %></p>
<div class="mt-4 flex gap-3">
  <a href="/posts/<%= post.id %>/edit" class="px-3 py-2 rounded-lg bg-neutral-800 text-neutral-100 border border-neutral-700">Edit</a>
  <form method="POST" action="/posts/<%= post.id %>?_method=DELETE" onsubmit="return confirm('Delete this post?');">
    <button class="px-3 py-2 rounded-lg bg-red-600 text-white">Delete</button>
  </form>
</div>
